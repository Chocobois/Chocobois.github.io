import { NavBar } from "@/components/nav-bar";

export default function Home() {

    return (<>
        <h1 className="text-5xl">Chocobois</h1>
        <NavBar />
        <p>We make game</p>
    </>);
}